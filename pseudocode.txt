to draw:

if this point is on curve (it should be)
	if next point is on curve
		draw line
		this point += 1
	else
		if NEXT next point is on curve
			draw bezier
			this point += 2
		else
			create midpoint
			draw bezier
			this point += 1
	
